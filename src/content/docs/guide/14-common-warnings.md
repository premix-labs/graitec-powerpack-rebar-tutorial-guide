---
title: Warning ที่เจอบ่อยและวิธีแก้แบบเร็ว
description: รวม warning และปัญหาที่ผู้เริ่มต้นเจอบ่อยใน PowerPack Rebar พร้อมวิธีเช็กแบบเร็ว
sidebar:
  order: 19
---

## 41. บทนี้ต่างจากบท Troubleshooting เดิมอย่างไร

บท `Workflow และการแก้ปัญหา` เน้นลำดับการฝึกและหลักคิดภาพรวม  
แต่บทนี้ทำเป็น "หน้าค้นเร็ว" สำหรับตอนที่คุณกำลังทำงาน แล้วเจอ warning หรือผลลัพธ์แปลกทันที

---

## 42. Warning: กดคำสั่งแล้วไม่มีอะไรเกิดขึ้น

### เช็กแบบเร็ว

1. เลือก host แล้วหรือยัง
2. host เป็น `Structural` หรือไม่
3. อยู่ในมุมมองที่เหมาะกับคำสั่งนั้นหรือไม่
4. element เป็นคอนกรีตจริงหรือไม่

### วิธีแก้

- เลือก element ใหม่
- เปิด `3D`, `Plan`, หรือ `Section` ที่เหมาะ
- เช็ก material และ category ของ host

---

## 43. Warning: สร้างแล้วแต่หาเหล็กไม่เจอ

### เช็กแบบเร็ว

1. `Detail Level = Fine` หรือยัง
2. ใช้ `Rebar Visibility` แล้วหรือยัง
3. ใน view นั้นมีการซ่อน rebar อยู่หรือไม่

### วิธีแก้

- เปิด `3D View`
- ใช้ `Rebar Visibility`
- ซูมไปที่ host ที่เพิ่งสร้างเหล็ก

---

## 44. Warning: Rebar is placed completely outside of its host

นี่คือ warning ที่เจอบ่อยมาก โดยเฉพาะงานผนัง และบางครั้งใน footing, supported element, หรือเหล็กปลายงอ

### เช็กแบบเร็ว

1. `Rebar Cover` ตรงกับค่า offset หรือไม่
2. มีช่อง offset ไหนยังเป็น `0` หรือไม่
3. hook หรือ shape ของเหล็กทำให้ปลายเลย host หรือไม่
4. host ถูก `Flip` ผิดด้านหรือไม่
5. host มี join หรือ geometry แปลกหรือไม่

### วิธีแก้

1. ตั้ง `Rebar Cover` ให้ชัดก่อน
2. ตั้ง offset ให้เท่ากับ cover ก่อน
3. ปิด hook ด้วยค่า `0` เพื่อทดสอบ
4. เพิ่ม offset เฉพาะด้านที่มีปัญหาทีละน้อย
5. ใช้ `Show` หรือ `Expand` เพื่อดูว่าเตือนเส้นไหน

### กรณี `Links` ของ supported element ลอยอยู่เหนือ host

ให้เช็กเพิ่มดังนี้

1. `Offset` ของ `Supported Element Transversal > Links` สูงเกินไปหรือไม่
2. `Lateral left / right / rear / front` ดันปลอกออกนอก host หรือไม่
3. supported element มีขนาดเล็กกว่าค่าที่ใส่อยู่หรือไม่

ลำดับแก้ที่ปลอดภัยคือ

1. ลด `Offset` ลงเป็น `0 mm`
2. คง `L1/L2/L3/L4` ไว้ที่ `40 mm` ก่อน
3. เช็กใน `3D` อีกครั้ง

### กรณี `Binders Bars` แล้วขึ้น warning

ถ้า warning มาจาก `Binders Bars Along X` หรือ `Binders Bars Along Y` ให้คิดไว้ก่อนว่า binder set นี้อาจไม่เหมาะกับ footing/pedestal ตัวนั้น

วิธีตัดสินใจเร็วที่สุดคือ

1. ใช้ `Show` ดูว่าเตือนเส้นไหน
2. ถ้าเป็น binder bar ที่ลอยนอก host ให้ลบ `Binders` ออกก่อน
3. คงไว้เฉพาะ `Pad - Bars`, `Supported Element Longitudinal`, และ `Links`

> [!WARNING]
> ใน footing พื้นฐาน `Binders` ไม่ใช่ชุดที่ต้องใส่ทุกครั้ง ถ้าใส่แล้วเกิด warning ให้เอาออกก่อน ไม่ต้องฝืนแก้จนเสียเวลา

---

## 45. Warning: spacing ที่ได้ไม่ตรงเลขที่พิมพ์

### ตัวอย่าง

- พิมพ์ `200 mm`
- โปรแกรมแสดง `197 mm`

### ความหมาย

ปลั๊กอินกำลังกระจายเหล็กให้ลงตัวกับระยะสุทธิจริงของ host

### วิธีตัดสินใจ

- ถ้าใช้ฝึก workflow ถือว่าใช้ได้
- ถ้างานจริงต้องการเลขตรงตามแบบ ให้กลับไปเช็ก distribution, quantity และ offset

---

## 46. Warning: Copy Rebar แล้วผลไม่เหมือนต้นแบบ

### เช็กแบบเร็ว

1. host ใหม่มีขนาดเท่าต้นแบบหรือไม่
2. `Rebar Cover` เท่ากันหรือไม่
3. orientation เท่ากันหรือไม่
4. host ปลายทางเป็น mirrored หรือกลับด้านหรือไม่

### วิธีแก้

- ทำต้นแบบให้เรียบที่สุดก่อน
- ใช้กับ host ที่ใกล้เคียงกันจริง
- หลัง copy แล้วให้เปิด `3D` หรือ `Section` ตรวจซ้ำเสมอ

### ถ้า copy ผ่านแต่ผลกลับด้าน

ให้สงสัยเรื่อง orientation หรือ mirror ก่อนเรื่อง spacing และ offset

---

## 47. Warning: ปุ่มบางเมนูเป็นสีเทา

### เช็กแบบเร็ว

1. เลือก host ถูกชนิดหรือไม่
2. host รองรับคำสั่งนั้นหรือไม่
3. มีค่าบางตัวที่ต้องเปิดก่อนหรือไม่ เช่น `Transverse reinforcement type`

### วิธีแก้

- เปลี่ยนไปเลือก host ที่เหมาะ
- เปิดค่าที่เกี่ยวข้องก่อน
- เริ่มจากเมนูพื้นฐานก่อน เช่น `Main Bars`

---

## 48. ถ้าต้องการเริ่มใหม่ให้เร็วที่สุด

1. ลบเหล็กด้วย `Delete Rebar`
2. เช็ก `Rebar Cover`
3. เช็ก `Detail Level = Fine`
4. เริ่มจากค่าตัวอย่างง่าย ๆ ก่อน
5. ตรวจใน `3D` ทันทีหลังสร้าง

---

## 49. สรุปแบบสั้นที่สุด

- ปัญหาส่วนใหญ่ไม่ได้มาจากปุ่ม แต่เกิดจาก `host`, `cover`, `offset`, `view`
- ถ้าเจอ warning ให้หาว่าเป็น "ทั้งชุด" หรือ "แค่บางเส้น"
- ใน footing ถ้า `Binders` พัง ให้ถอยกลับไปใช้แค่ `Pad - Bars`, `Supported Element Longitudinal`, และ `Links`
- อย่าเริ่มจากค่าซับซ้อน ให้เริ่มจากค่ากลาง ๆ ก่อนเสมอ
